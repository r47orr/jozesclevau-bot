const discord = require('discord.js');

module.exports = {
    name = 'ping_pong',
    aliases = ['ping', 'test'],
    cooldown = 2000,
    help = 'Retorna o tempo de resposta do bot em relação à API do Discord',
    usage = '\`${}\`',
}